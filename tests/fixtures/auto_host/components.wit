package game:components;

interface components {
  use wasvy:ecs/app.{component};

  /// wasvy:type-path=tests::fixtures::components::Health
  resource health {
    constructor(component: component);
    pct: func() -> f32;
  }
}

world host {
  import components;
}
