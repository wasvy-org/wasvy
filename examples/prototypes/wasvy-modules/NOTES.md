# Prototype Notes

Question answered:

- _Fill this in after driving the prototype._

Things to watch for:

- Does **World Composition** feel like the right boundary?
- Does **First-load Initialization** once-per-world make sense in practice?
- Does the blocked reload behavior feel understandable?
- Does preserving shared/private world state while swapping generations feel like the right default?

When this prototype has done its job:

- either delete it, or
- absorb the validated runtime model into the real Wasvy Modules implementation work.
